#!/usr/bin/env perl

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";
use Encode::Wide qw(wide_to_html);

die "Usage: wide2html <string>\n" unless @ARGV;

print wide_to_html(string => join(' ', @ARGV)), "\n";
