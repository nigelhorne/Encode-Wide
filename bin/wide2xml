#!/usr/bin/env perl

use strict;
use warnings;

use FindBin;
use lib "$FindBin::Bin/../lib";
use Encode::Wide qw(wide_to_xml);

die "Usage: wide2xml <string>\n" unless @ARGV;

print wide_to_xml(string => join(' ', @ARGV)), "\n";
